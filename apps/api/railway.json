{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "preDeployCommand": ["sh pre-deploy.sh"],
    "startCommand": "/start-container.sh",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 120
  }
}
