# Current-site migration inventory

Initial source: https://niwadu.com/

The local snapshot is stored outside git at `/Users/rashmiassiriyage/niwadu-rebuild/source-snapshot`.

- Homepage HTML captured.
- 52 distinct internal link references discovered in homepage anchors.
- 44 image references discovered: 43 Niwadu-hosted assets downloaded; one third-party tracking image excluded.
- 18 stylesheet references discovered; not yet downloaded.
- robots.txt permits crawling. The conventional /sitemap.xml URL returns HTTP 404.

This is a homepage inventory, not a complete crawl. Next capture hotel, destination, experience and information pages, including lazy-loaded galleries, responsive image variants and CSS/font assets. Retain source URLs and hashes to identify duplicates and support migration validation. Public HTML does not supply unpublished records or necessarily original-resolution images.

Before implementation, capture matched desktop/mobile screenshots and interaction states. Before launch, compare migrated listing counts, media mappings and URL coverage with an authoritative data export where available.
