# Public account and private district map candidate

Routes: /account (sign in/current account), /account?mode=register, and archive-verified /cover. Header Profile/Log In/Sign Up and coverage promo/menu/footer entries now point to these internal routes. No employee/staff control is exposed on the public account screen; existing /admin code remains untouched.

PO approved the bounded contract with Admin. Existing GET session and POST login/logout use same-origin credentials and CSRF. Registration POST register sends only name,email,password,password_confirmation; backend must enforce null platform_role/no memberships, reject role injection and duplicate email without mutation. Client obtains a fresh CSRF token and refuses login/register over an already signed-in account. API role authorization remains a backend responsibility. Exact register response may be201 or200; frontend refetches session on /cover, never treats a response as proof of a privileged role.

GET/PUT me/coverage uses top-level {districts:string[],version:number}. PUT is full replacement with known25 reference district slugs and expected version; backend enforces own-user scope and transaction/version isolation. Frontend validates returned version/unique slugs, keeps edits in memory only, saves explicitly, prevents duplicate in-flight writes and compares the current session identity before writes. It checks identity before/after coverage reads and after save, and on window/visibility/history return.401/419 clears private state. Request generations prevent late coverage responses repopulating a cleared identity. Session and coverage reads are no-store; no personal map content enters server-rendered HTML or localStorage.

409 and ambiguous save failure retain selections and block automatic retry. Compare saved map refetches current state; user explicitly chooses saved map or keeps local selections, then Save is a separate action. Reset affects local selection until explicit Save. Dirty navigation uses the browser unsaved-changes prompt. No guest import, query-string sharing, public profile, booking/payment, hotel privilege, or account-backed planner claim. The existing /plan remains a separate browser-local itinerary.

Functional tests intercept only local browser API requests with synthetic fixtures: explicit save/persistence, no localStorage, version conflict/explicit reconciliation, wrong-account preflight/no write, public registration allowlisted payload, logout, blocked service/retry, mobile320, empty reset, expired PUT, ambiguous success-response loss and registration field validation. Existing public/planner/carousel/image scenarios retained. These tests prove frontend behavior only; real registration authorization, session/CSRF boundaries, per-user isolation and durable persistence require independent integrated QA against Admin's immutable API commit before publication. No hosted accounts or writes were created.

## Registration password correction

Independent real integration passed API6020f3d/frontend279da07 (QA ec69fc8), but exposed inherited bcrypt truncation past72bytes despite API max1024. PO blocks registration release until server enforces the bound. Frontend now checks minimum12 Unicode code points and maximum72 UTF-8 bytes for registration only, explains multibyte characters, preserves the entire entered value and focuses the invalid field. It does not use maxlength72 or silently truncate. Login continues sending existing passwords unchanged for legacy compatibility. Backend validation remains mandatory; client validation is not the security boundary. Regression checks74byte rejection, exact72byte multibyte acceptance and unchanged overlength login payload.
