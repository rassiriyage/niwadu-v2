# Metadata discovery frontend

Routes: /hotels and /hotels/[slug], matching the inspected archive. Next server reads API_ORIGIN/api/v1/public/hotels, discovery-options and hotels/{slug}; no-store fetches and force-dynamic pages. No credentials forwarded to public reads. API_ORIGIN is server-only. Missing backend returns an explicit unavailable/retry state, never reference or synthetic listings.

Wire source: Catalog owner's PublicHotelController and PublicHotelResource metadata-only contract. Public fields id,slug,name,description,property_type,city,country,photo:null. Options {data:{property_types:[{key,label}],capabilities}}; detail {data:hotel}; Laravel list {data:hotel[],meta:{current_page,last_page,total,per_page,...}}. Backend owns administrator-approved snapshot/release/withdrawal, classification and source truth. No real source release is authorized by this frontend work.

PO explicitly approved interim Name A–Z (sort=name). Name search is literal property-name search, not destination/availability search. Types OR within the selected set, supplied by the options endpoint rather than duplicated client taxonomy. Canonical local query URLs use sorted/deduplicated property_types[] keys; page1 is omitted and filter changes reset pagination. Repeated scalars, unknown/legacy parameters, unsupported sorts, invalid page bounds or unknown type choices get explicit recovery; never silently substitute recommended order or broaden their result set. Tracking query keys are not sent to the API. No date/price/star/occupancy filters until backed by approved data.

Filters use a native modal with Apply/Cancel/reset-to-applied behavior. Active chips remove individual filters; search URLs survive refresh and Back. FreshLink uses Next Link without prefetch and full-origin navigation to avoid replaying client-router snapshots. A persisted browser history restore reloads from origin. Subsequent reads must omit withdrawn listings; already disclosed content cannot be recalled. Unknown/withdrawn detail has a not-found state. No private images or shared optimizer cache enter this metadata slice; neutral 'Photo not available' is intentional.

Verification: independent test-only HTTP fixture under tests/fixtures/discovery-server.mjs, loopback8099, synthetic QA labels, never imported by application code. playwright.discovery.config.ts starts that server and a production frontend3209. Four cases cover raw intent/canonical arrays, filter Cancel/Apply, pagination/detail, empty/error/retry, and withdrawal after browser Back at320px. This is frontend contract evidence, not backend authorization/transaction proof. Integrated QA against exact Catalog API remains required before feature publication. Existing public/carousel/planner checks use playwright.public.config.ts with separate production3208.

Catalog clarification: valid six-type filters without an active release return200/empty; options only lists active types. Preserve such a selected key in the chip and filter checkbox (labelled no current listings), including Apply/Cancel. Backend422 distinguishes unknown keys. Added an explicit empty-type regression.

## Dated room-rate lookup (separate from accepted 47c4c3c)

Hotel details now offer a native GET form for arrival, departure and adult count, one room. The server reads accepted Catalog `09501b7928f47d35a1f6205b8f1764aef3f2195d` public rate-plan endpoint with no-store. Submitted URL choices survive refresh; unknown/repeated fields fail visibly rather than broadening the query. API validation remains authoritative for dates, occupancy and 1–30-night limits. Results show room/plan, LKR stay total including mandatory charges, capacity, policy and explicit check expiry; no reservation or payment action is exposed. Empty and unavailable states keep the form usable. This is a rate lookup, not planner stay selection or account plan persistence.

Local verification: production build/TypeScript and lint; five existing discovery regressions plus two new synthetic HTTP-fixture browser scenarios covering totals, URL reload, 320px overflow, empty results, invalid/repeated choices, service failure and retry. Fixture rates are test-only and never imported by application code. Exact API integration and independent QA remain required before publication. This commit must stay separate from accepted account/discovery/password baseline47c4c3c, which can publish independently after the existing backend deployment gate.

## Expanded metadata filters — contract candidate

Implemented against Catalog's frozen next contract, whose backend implementation and independent acceptance are still pending: scalar destination/district/region, theme OR selections, amenity ALL selections, existing property-type OR selections, AND across groups. Options come from active reviewed API snapshots. Chosen absent options remain visible for recovery; unknown values are rejected by the API. Search/filter/sort forms retain other choices, reset page, support Apply/Cancel, removable chips and clear. Editorial sorting is offered only when advertised; requested unsupported editorial intent remains explicit and fails instead of silently changing sort. Name A–Z remains the selected default.

Archive dest, six category themes, category=north and sort=popular map to the frozen canonical destination/themes/region/editorial semantics. Homepage category chips now link to these queries. Old price/availability/star parameters remain unsupported; no stars are inferred. Detail fields show only reviewed destination, district, themes and amenities returned by the API. Dates stay on the separate rate endpoint.

Local production build/lint and nine fixture discovery browser cases pass, including combined choices, search/sort preservation, cancellation, page reset, aliases, unavailable editorial recovery and earlier dated lookup. Fixtures are not evidence of real backend facet semantics; exact Catalog integration QA and deployment readiness are still required. This follow-on must not be published with accepted47c or the separate5e dated-rate slice until those gates clear.

## Exact dated currency and meal plans — 2026-09-26

The header now offers LKR/USD, stored as a non-sensitive browser preference cookie. Explicit detail URL currency takes precedence; the GET rate form includes currency and preserves dates/adults. The deployed API f01dacc contract supplies independently entered minor-unit amounts (exponent 2 for both currencies), not FX conversions. A response in a different currency is rejected. Missing selected-currency rates remain empty. RO/BB/HB/FB have explicit labels; null remains “Meal plan not specified”. Currency alone does not trigger an incomplete date search. Booking/payment remains unavailable.

Validation: production build and lint pass; existing nine discovery browser checks pass, followed by the new focused currency journey passing after correcting its select locator to the accessible combobox role. The new test first failed against the prior build because no Currency selector existed. Covers browser preference, explicit URL, USD/LKR totals, BB/null labels, missing USD, reload and invalid/repeated currency. Synthetic loopback fixtures only; no hosted writes.
