
## Manual inventory and PMS configuration UI candidate

Isolated `feature/admin-inventory` starts from accepted frontend47c4c3c. New `/admin/hotels/{id}/inventory` uses the existing authenticated workspace, with profile and draft step4 links (existing draft save-before-navigation retained). Room detail → explicit manual ownership → rate plan/policy → seven-day stock/rate editor. Missing rows stay unknown, prices convert decimal LKR exactly to integer minor units, all charges require explicit input, incomplete charge state and checkout-date restrictions are explained. No room-photo conversion, publishing, holds, payment or live PMS action.

Hotel permission flags from operations API govern room, inventory and platform-only PMS controls; backend gates remain authoritative. PMS settings use masked replacement credentials, never populate returned secrets, and distinguish saved settings from connection verification. Input stays in memory, save/401/419/session mismatch clears credentials; no private browser storage. Writes check session identity before and after, stale/uncertain writes retain inputs and block automatic replay until explicit discard/reload. Unsaved navigation/signout have confirmation; in-flight view switches are blocked.

API runtime10e7e27 passed independent SQLite88/772 and guarded PG60/644, plus real public dated-rate pairing. API5aa4c35 adds only guarded OperationsBrowserTestSeeder with separate fixture accounts to retain real throttling. UI Node24 lint/typecheck/production build and8 real Chromium tests pass: full room/pool/plan/stock/money flow+reload, real409, viewer denial, inventory-manager scope, PMS redaction, session expiry, uncertain write/unchanged-version reset, unsaved navigation+keyboard. Screenshots checked320/768/1024/1440; no body overflow, table intentionally scrolls horizontally on narrow screens. Local test config uses API8233/frontend3233 and a dedicated SQLite browser-test database; no hosted writes. Comprehensive design review remains after functional completion.

## Expanded first-setup wizard — 2026-09-25

`feature/admin-room-wizard` extends accepted UI `2b3c58b`. Step 2 contains only the property gallery. Step 3 creates real room IDs and shows each room's own photo gallery. Both galleries have accessible Move earlier/later controls, persist their independent order, use expected versions, and require explicit GET reconciliation after uncertain writes. Room creation supplies a stable UUID retained with its original payload for interrupted-response recovery. Existing draft arrays require an explicit conversion action; their indicative prices and quantities are not promoted to sellable inventory.

Step 4 offers optional RO, BB, HB and FB, each with independently entered LKR and USD dated prices. These offers share the room's inventory pool. Blank policy and money components remain unknown; completing charges requires explicit amounts, with zero entered only when confirmed. Onboarding employees author drafts but cannot activate inventory, active offers or PMS settings. The existing inventory editor now renders actual currencies and nullable amounts correctly.

Room, policy and nightly inputs autosave with individual versions. Per-user/hotel tab recovery retains incomplete inputs, selected room/offer/date and creation UUID. Conflicts preserve inputs and require explicit reload; unknown writes never replay automatically. Session expiry and sign-out clear recovery. General hotel autosave continues while a photo is selected, while navigation requires the selected upload to be handled. Existing staff/password and hotel-field conflict flows remain in place.

API pair: `f01daccaa17a4b39e8f974f1344a2632f933a146` in `api-wizard`, combining accepted operations/discovery/itineraries/deployment guards with Catalog `a1f41a6` and gallery order `4e21a02`. Modern session middleware, permissions, UTC connection setting and explicit migration rollback paths are preserved. Local SQLite: 110 tests / 1,117 assertions and full Pint pass. Independent PostgreSQL: 82 / 1,012 passed on clean rerun after transient migration deadlocks; Pint/diff passed. This contains no booking/payment runtime expansion.

Browser configuration: `playwright.wizard-rooms.config.ts`, API 8244 / frontend 3244, disposable SQLite `database/niwadu-browser-tests.sqlite`, guarded OperationsBrowserTestSeeder. It sets WIZARD_API_PATH for password-log fixtures. Create `storage/framework/testing` and the empty test database first; build the UI with API_ORIGIN pointing at 8244. No hosted identities, invitations, payments or PMS calls are used. Final immutable UI and independent paired acceptance follow local verification; comprehensive design review and deployment remain separately coordinated.

Local verification completed: lint, TypeScript and production build pass. The 21-case wizard/onboarding/password run passed 20, identifying loss of keyboard focus during a validation autosave; the focus fix then passed its targeted rerun. Existing inventory/PMS regressions passed 8/8, and an additional real-API USD/null-charge compatibility test passed. Thus 30 distinct cases have passing evidence; independent QA must rerun the full exact-hash pair. Screenshots at 320/768/1024/1440 have no horizontal overflow; 320 and 1440 were visually inspected. Scoped source graphs were refreshed. No push or hosted deployment occurred.
