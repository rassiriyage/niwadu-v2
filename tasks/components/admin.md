
## Manual inventory and PMS configuration UI candidate

Isolated `feature/admin-inventory` starts from accepted frontend47c4c3c. New `/admin/hotels/{id}/inventory` uses the existing authenticated workspace, with profile and draft step4 links (existing draft save-before-navigation retained). Room detail → explicit manual ownership → rate plan/policy → seven-day stock/rate editor. Missing rows stay unknown, prices convert decimal LKR exactly to integer minor units, all charges require explicit input, incomplete charge state and checkout-date restrictions are explained. No room-photo conversion, publishing, holds, payment or live PMS action.

Hotel permission flags from operations API govern room, inventory and platform-only PMS controls; backend gates remain authoritative. PMS settings use masked replacement credentials, never populate returned secrets, and distinguish saved settings from connection verification. Input stays in memory, save/401/419/session mismatch clears credentials; no private browser storage. Writes check session identity before and after, stale/uncertain writes retain inputs and block automatic replay until explicit discard/reload. Unsaved navigation/signout have confirmation; in-flight view switches are blocked.

API runtime10e7e27 passed independent SQLite88/772 and guarded PG60/644, plus real public dated-rate pairing. API5aa4c35 adds only guarded OperationsBrowserTestSeeder with separate fixture accounts to retain real throttling. UI Node24 lint/typecheck/production build and8 real Chromium tests pass: full room/pool/plan/stock/money flow+reload, real409, viewer denial, inventory-manager scope, PMS redaction, session expiry, uncertain write/unchanged-version reset, unsaved navigation+keyboard. Screenshots checked320/768/1024/1440; no body overflow, table intentionally scrolls horizontally on narrow screens. Local test config uses API8233/frontend3233 and a dedicated SQLite browser-test database; no hosted writes. Comprehensive design review remains after functional completion.

## Expanded first-setup wizard — 2026-09-25

`feature/admin-room-wizard` extends accepted UI `2b3c58b`. Step 2 contains only the property gallery. Step 3 creates real room IDs and shows each room's own photo gallery. Both galleries have accessible Move earlier/later controls, persist their independent order, use expected versions, and require explicit GET reconciliation after uncertain writes. Room creation supplies a stable UUID retained with its original payload for interrupted-response recovery. Existing draft arrays require an explicit conversion action; their indicative prices and quantities are not promoted to sellable inventory.

Step 4 offers optional RO, BB, HB and FB, each with independently entered LKR and USD dated prices. These offers share the room's inventory pool. Blank policy and money components remain unknown; completing charges requires explicit amounts, with zero entered only when confirmed. Onboarding employees author drafts but cannot activate inventory, active offers or PMS settings. The existing inventory editor now renders actual currencies and nullable amounts correctly.

Room, policy and nightly inputs autosave with individual versions. Per-user/hotel tab recovery retains incomplete inputs, selected room/offer/date and creation UUID. Conflicts preserve inputs and require explicit reload; unknown writes never replay automatically. Session expiry and sign-out clear recovery. General hotel autosave continues while a photo is selected, while navigation requires the selected upload to be handled. Existing staff/password and hotel-field conflict flows remain in place.

API pair: `f01daccaa17a4b39e8f974f1344a2632f933a146` in `api-wizard`, combining accepted operations/discovery/itineraries/deployment guards with Catalog `a1f41a6` and gallery order `4e21a02`. Modern session middleware, permissions, UTC connection setting and explicit migration rollback paths are preserved. Local SQLite: 110 tests / 1,117 assertions and full Pint pass. Independent PostgreSQL: 82 / 1,012 passed on clean rerun after transient migration deadlocks; Pint/diff passed. This contains no booking/payment runtime expansion.

Browser configuration: `playwright.wizard-rooms.config.ts`, API 8244 / frontend 3244, disposable SQLite `database/niwadu-browser-tests.sqlite`, guarded OperationsBrowserTestSeeder. It sets WIZARD_API_PATH for password-log fixtures. Create `storage/framework/testing` and the empty test database first; build the UI with API_ORIGIN pointing at 8244. No hosted identities, invitations, payments or PMS calls are used. Final immutable UI and independent paired acceptance follow local verification; comprehensive design review and deployment remain separately coordinated.

Local verification completed: lint, TypeScript and production build pass. The 21-case wizard/onboarding/password run passed 20, identifying loss of keyboard focus during a validation autosave; the focus fix then passed its targeted rerun. Existing inventory/PMS regressions passed 8/8, and an additional real-API USD/null-charge compatibility test passed. Thus 30 distinct cases have passing evidence; independent QA must rerun the full exact-hash pair. Screenshots at 320/768/1024/1440 have no horizontal overflow; 320 and 1440 were visually inspected. Scoped source graphs were refreshed. No push or hosted deployment occurred.

## Staff access feedback — 2026-09-26

The staff form now reads `password_setup_sent` from a confirmed grant response. It reports a sent link only when the API confirms one; otherwise it states that access was saved without a link and points to the explicit password-link action. A subsequent failed roster refresh preserves the confirmed grant message and asks the operator to reload the page, rather than suggesting that access must be granted again. Authorization and delivery behavior are unchanged.

Three real-API browser regressions reproduced the old feedback: an existing account, a new account with log-only password mail, and a successful grant followed by an injected roster-read failure. The added test file is included in the existing isolated wizard configuration. No hosted account, invitation or transaction is created by this change.

Validation: all 33 wizard/onboarding/staff/password/inventory browser tests passed against API `f01dacc` in 2.4 minutes. Lint, TypeScript, production build and diff checks passed. Output is isolated at `/tmp/niwadu-staff-feedback-full-20260926`; the initial three failing regressions are retained separately. The first green run exposed only an ambiguous test selector matching Next's empty route announcer; scoping the alert to the staff panel resolved it. Publication remains coordinated separately.

Hosted status: accepted UI `499ee401` and API `f01dacc` deployed successfully and guest QA passed. A read-only Railway SSH recheck still reports no registered SSH keys; authenticated hosted wizard verification remains pending operator console/access and the previously authorized disposable account. No account creation was attempted and no keys were registered.

Next bounded UI work: link Review missing-content items to their owning steps using the existing save-before-navigation path; compact mobile progress/conflict recovery without weakening preserved-input disclosure. Design withdrew the stale immediate room-removal finding in `8e203df`: that control is absent from the accepted expanded wizard.

## Review navigation and mobile conflict recovery — 2026-09-26

Review renders an edit control for every current API missing-content label, using the existing `navigate` function to flush pending input and persist the target step before focusing its heading. Unknown future labels stay visible as text. Publication blockers remain unchanged.

The mobile progress rail keeps all seven labels and normal touch targets in a horizontally scrollable, keyboard-focusable region. Conflict warnings stay visible; native details disclosure contains the existing copy, explicit discard/reload, and unsaved JSON controls. No autosave, conflict version, room identity or photo mutation behavior changes.

Validation: all 36 browser cases passed against API `f01dacc` in 2.0 minutes, including keyboard edit navigation through six owning steps, navigation blocked by a changed server draft, copy and failed reload recovery, browser Back/Forward recovery, and responsive disclosure checks. Lint, TypeScript, production build and diff checks passed. The initial missing-edit and mobile-position tests failed before implementation. Intermediate runs retained an over-height layout and an old recovery test that needed to open the new disclosure; both are resolved in the final run.

At 390 × 844, the first ordinary input's bottom is asserted within 844 pixels (approximately 837 in the screenshot), compared with approximately 1,619 before. The mobile rail is focusable and scrolls with ArrowRight; all seven buttons remain at least 44 pixels tall. Disclosure expand/collapse retains keyboard focus and preserves unsaved JSON. No page overflow at 320, 768, 1024 or 1440 pixels; screenshots at those widths and 390 were visually inspected. Final artifacts: `/tmp/niwadu-review-conflict-final-20260926`. This is local browser evidence, not a hosted authenticated or comprehensive screen-reader/usability sign-off.

The candidate follows staff-feedback `d4d1f73`, remains unpublished, and requires independent QA/coordinated frontend integration. The hosted SSH/account blocker is unchanged.

### Independent focus correction

QA blocked `0bc7985` because a failed Review edit rendered the Save problem region without focusing it. The same case failed in three isolated owner reproductions despite the earlier full-suite pass. Navigation focus had been scheduled with `requestAnimationFrame`, outside React's commit timing. Navigation and explicit reload now request either heading or summary focus through a layout effect after the target commits; background autosave does not request focus. Repeated navigation against the same stored conflict is covered.

The first repetition run also exposed a late successful-navigation focus issue and login throttling from repeatedly sharing the admin fixture. Heading focus is now committed through the same layout effect, and mobile/conflict cases use distinct existing seeded accounts. Login throttling and test timeouts are unchanged. This correction must pass independent QA before the Review/conflict slice is integrated or published.

Final correction validation: 12/12 focused repetitions passed in 49.2 seconds, followed by all 36 browser cases in 2.8 minutes against clean API `f01dacc`. Production build, TypeScript, changed-file ESLint and diff checks passed. The first full rerun passed 35/36: the session-expiry test logged out before the room fetch finished, and the app correctly cleared the editor before the test attempted to fill it. The test now waits for the actual room field before logout, preserving the real session-expiry and private-recovery assertions. No runtime auth behavior or timeouts changed. Artifacts: `/tmp/niwadu-review-focus-final-focused-20260926` and `/tmp/niwadu-review-focus-verified-full-20260926`; failed runs are retained separately. Independent acceptance of the successor remains required.
